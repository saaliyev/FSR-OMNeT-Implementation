//
// package.ned
// Defines the package for the OMNeT++ project.
// This should be placed in the root of your OMNeT++ project directory.
//
package fsr;

// Specifies the version of the NED language.
@nek;
